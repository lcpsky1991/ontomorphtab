-- Schema for Whole Brain Catalog's Database
-- author: Stephen D. Larson (slarson@ncmir.ucsd.edu)

AUTOCOMMIT OFF;

CREATE TABLE USERS
   (
      USER_ID CHAR(2) NOT NULL ,
      USER_NAME VARCHAR(24),
   );


COMMIT;
